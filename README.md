# where-we-left-off.sh
  where-we-left-off.sh: A linux utility for performing various file tree operations in a way that can resume if interrupted by hardware issues.  Performs the jobs of diff, rsync, du, and tree in an interruptable form.
